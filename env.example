HOST=0.0.0.0  # 配置 NoneBot 监听的 IP / 主机名
PORT=8080  # 配置 NoneBot 监听的端口
COMMAND_START=["/"]  # 配置命令起始字符
COMMAND_SEP=["."]  # 配置命令分割字符

# 超级用户配置
SUPERUSERS=["你的QQ号"]

# B站直播通知插件配置
# 群组ID列表（用逗号分隔多个群组）
STREAM_NOTIFY_NOTIFY_GROUPS=["123456789", "另一个群组ID"]

# 是否包含房间信息
STREAM_NOTIFY_INCLUDE_ROOM_INFO=true